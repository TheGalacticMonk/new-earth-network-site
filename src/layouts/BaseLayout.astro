---
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';
// Resolved, hashed URLs for the above-the-fold fonts' Latin subset — see the
// preload <link>s below for why these two, and why the Latin subset
// specifically.
import spaceGroteskLatinUrl from '@fontsource-variable/space-grotesk/files/space-grotesk-latin-wght-normal.woff2?url';
import manropeLatinUrl from '@fontsource-variable/manrope/files/manrope-latin-wght-normal.woff2?url';
// Favicons live here (not public/) so their URL is content-hashed on every
// build — a plain public/ URL keeps its filename across content changes, and
// browsers cache favicons far more stubbornly than ordinary page resources
// (a hard refresh doesn't reliably bust it). A changed hash forces a fresh
// fetch automatically, so a future favicon swap can't hit this same problem.
import favicon32Url from '../assets/favicon-32.png?url';
import favicon16Url from '../assets/favicon-16.png?url';
import appleTouchIconUrl from '../assets/apple-touch-icon.png?url';

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
const siteName = 'New Earth Network';
const pageTitle = title === siteName ? title : `${title} — ${siteName}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- Preload the Latin subsets used above the fold: Manrope for the hero
         and form, Space Grotesk for navigation. Other fonts load when needed. -->
    <link rel="preload" as="font" type="font/woff2" href={spaceGroteskLatinUrl} crossorigin />
    <link rel="preload" as="font" type="font/woff2" href={manropeLatinUrl} crossorigin />
    <link rel="icon" type="image/png" sizes="32x32" href={favicon32Url} />
    <link rel="icon" type="image/png" sizes="16x16" href={favicon16Url} />
    <link rel="apple-touch-icon" href={appleTouchIconUrl} />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <meta name="theme-color" content="#140A1D" />

    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:site_name" content={siteName} />
    <meta name="twitter:card" content="summary" />
  </head>
  <body>
    <Navigation />
    <main id="main">
      <slot />
    </main>
    <Footer />
  </body>
</html>
