---
import Container from './Container.astro';
import Button from './Button.astro';

interface Props {
  kicker?: string;
  heading: string;
  primaryLabel: string;
  primaryHref: string;
  secondaryLabel?: string;
  secondaryHref?: string;
  tone?: 'primary' | 'deep';
}

const { kicker, heading, primaryLabel, primaryHref, secondaryLabel, secondaryHref, tone = 'deep' } = Astro.props;
---

<div class="cta" data-tone={tone}>
  <Container narrow>
    <div class="cta-inner">
      {kicker && <p class="kicker">{kicker}</p>}
      <h2 class="heading">{heading}</h2>
      <div class="actions">
        <Button href={primaryHref} variant="primary" size="lg">{primaryLabel}</Button>
        {secondaryLabel && secondaryHref && (
          <Button href={secondaryHref} variant="secondary" size="lg">{secondaryLabel}</Button>
        )}
      </div>
      <slot />
    </div>
  </Container>
</div>

<style>
  .cta {
    padding-block: var(--space-24);
    position: relative;
  }

  .cta[data-tone='deep'] {
    background: var(--gradient-veil);
  }

  .cta-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-6);
    text-align: center;
  }

  .kicker {
    font-family: var(--font-accent);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-widest);
    text-transform: uppercase;
    color: var(--color-electric-cyan);
  }

  .heading {
    font-size: var(--text-xl);
    max-width: 20ch;
  }

  .actions {
    max-width: 100%;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: var(--space-4);
    margin-top: var(--space-2);
  }

  .actions :global(.btn) {
    max-width: 100%;
    white-space: normal;
    overflow-wrap: anywhere;
  }
</style>
